@model Product

<div class="col-lg-3 col-md-4 col-12 col-sm-6">
    <div class="product-cart-wrap mb-30">
        <div class="product-img-action-wrap">
            <div class="product-img product-img-zoom">
                <a asp-controller="Product" asp-action="Detail" asp-route-slug="@Model.Slug">
                    <img class="default-img" src="@Model.Image" alt="">
                    <img class="hover-img" src="@Model.Image" alt="">
                </a>
            </div>
            @* <div class="product-action-1">
            <a aria-label="Quick view" class="action-btn hover-up" data-bs-toggle="modal" data-bs-target="#quickViewModal"><i class="fi-rs-eye"></i></a>
            <a aria-label="Add To Wishlist" class="action-btn hover-up" href="shop-wishlist.html"><i class="fi-rs-heart"></i></a>
            <a aria-label="Compare" class="action-btn hover-up" href="shop-compare.html"><i class="fi-rs-shuffle"></i></a>
            </div> *@
            <div class="product-badges product-badges-position product-badges-mrg">
                @if (Model.IsHot)
                {
                    <span class="hot">Hot</span>
                }
            </div>
        </div>
        <div class="product-content-wrap">
            @if (Model.Categories.Count() > 0)
            {
                <div class="product-category">
                    <a href="shop-grid-right.html">@Model.Categories[0].Name</a>
                </div>
            }

            <h2><a asp-controller="Product" asp-action="Detail" asp-route-slug="@Model.Slug">@Model.Name</a></h2>
            <div class="rating-result" title="90%">
                <span>
                    <span>90%</span>
                </span>
            </div>
            <div class="product-price">
                <span>@Model.GetPrice() </span>

                @if (Model.CompareAtPrice > 0)
                {
                    <span class="old-price">@Model.GetCompareAtPrice()</span>
                }
            </div>
            <div class="product-action-1 show">
                <a aria-label="Add To Cart" class="action-btn hover-up" href="shop-cart.html"><i class="fi-rs-shopping-bag-add"></i></a>
            </div>
        </div>
    </div>
</div>